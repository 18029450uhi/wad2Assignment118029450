import React, {useEffect, useRef} from 'react';

function ALevelQuestions() {

    return (
        <div>
            <iframe
                src="http://localhost:4000"
                title="A-Level Questions"
                style={{width: '100%', height: '100vh'}}
            />
        </div>
    );
}

export default ALevelQuestions;
